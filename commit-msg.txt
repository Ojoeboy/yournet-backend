Fix stored XSS in license-admin, add custom duration to manual access, fix broken email verification flow, and fix top-right nav bar layout on Setup/Agents

- Escape buyer_email, buyer_phone, payment_method, notes, momo_reference,
  and key_code before innerHTML in license-admin.html; move quickEmail/
  copyKey off inline onclick strings onto data-* attributes to close a
  stored XSS hole reachable via the public claim-manual endpoint

- Add custom duration option (minutes/hours/days/months + number input)
  to manual IP/MAC binding on the Access panel, alongside existing presets

- Fix email-change verification link in dashboard.js pointing to an
  unmounted /verify-email route (404) instead of /api/auth/verify-email

- Split GET /api/auth/verify-email into a non-mutating token check that
  renders a confirm page, and a new POST that performs the actual update
  — prevents email security scanners from silently burning single-use
  verification tokens before the user clicks

- Add POST /api/dashboard/account-info/email-change/cancel plus a
  "Cancel pending change" button, so a tenant can back out of a pending
  email change without needing another valid address to overwrite it

- Fix top-right profile/language bar on admin.html and agents.html: the
  blanket `input,select{width:100%}` form-styling rule was stretching the
  injected language <select> to fill the whole top bar, shoving the
  profile button off to the left instead of both sitting together on the
  right like every other page. Scoped with #yournetLangSwitcher{width:auto}