fix: repair fixed-position elements trapped by zoom scaling
feat: add owner-only refined branding across owner pages

Bug fixes
- pppoe.html: move plan/subscriber/credential/payment modals to be
  siblings of </main> instead of nested inside .app-main, so they're
  no longer caught inside the zoomed containing block (same root
  cause as the earlier bottom-nav-vanishing fix)
- portal.html: inline font-size widget now scales .card instead of
  document.documentElement, preventing the fixed A+/A- widget itself
  from drifting under non-default zoom levels
- shell.js: fixed a race between ensureFavicon() and ensurePwaMeta() -
  ensureFavicon() was unconditionally injecting a tenant
  apple-touch-icon before the owner-aware logic in ensurePwaMeta()
  ran, so the four owner pages (license-admin, owner-revenue,
  owner-tenants, owner-media) were silently showing the tenant
  home-screen icon instead of the owner one; ensureFavicon() now only
  sets the browser-tab favicons, leaving apple-touch-icon to the
  owner/tenant-aware code exclusively

Owner branding (scoped to owner pages only - tenant assets/pages
untouched)
- New owner-only assets: public/img/logo-owner.png (full badge) and
  public/img/logo-icon-owner.png (small emblem), cropped from the
  refreshed artwork and kept fully separate from the shared tenant
  logo.png/logo-icon.png
- owner-login.html: restyled to match the tenant login's polished
  card (gradient card, shadow, teal focus states, gold gradient
  button), now shows the new badge and "YourNet Owner" branding with
  an updated subtitle; all existing login/PWA-install functionality
  and element IDs unchanged
- shell.js: added a brand() helper that switches the logo + label
  based on isOwnerMode() - applied to the desktop topbar brand mark,
  the mobile drawer header brand mark, and the per-page header logo
  (renderPageHeader) - so every owner page (license-admin,
  owner-revenue, owner-tenants, owner-media) now shows "YourNet
  Owner" and the new badge everywhere the tenant app shows its own
  logo, with zero change to tenant-side rendering