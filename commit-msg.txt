Unify left sidebar: icon-rail default, logo toggle, fixed scroll-to-Help bug

- Replace 4-bar hamburger with uploaded logo as the sidebar toggle button
  (public/shell.js, public/img/sidebar-toggle.png)
- Default sidebar mode to the slim 56px icon rail on first load instead of
  the always-expanded 220px rail; existing users who pinned it open keep
  that choice (public/shell.js: isAutoMode)
- Show nav item icons on the collapsed rail instead of hiding the whole
  nav; labels hide, tooltips added via title attr (public/shell.css,
  public/shell.js: navLinkHtml)
- Fix nested-scroll bug where the outer sidebar scrollbar swallowed the
  inner nav's scroll, cutting off the Help section - sidebar now clips,
  .app-nav owns its own scroll via min-height:0 (public/shell.css)

Applies to all pages sharing shell.js/shell.css: dashboard, agents,
admin, billing, pppoe, print, plan-overview.