Add portal logo upload, "use account logo", and client-side resize/crop

- POST /api/sites/:id/portal-logo: multer memory upload (1.5MB limit,
  PNG/JPEG/WEBP/GIF), base64-encodes into sites.portal_logo_url —
  same never-touches-disk pattern as the account logo, so it survives
  Render free-tier redeploys
- logoUrl added to CLEARABLE_PORTAL_FIELDS so the portal logo can
  actually be cleared, not just set (old PATCH /portal used COALESCE)
- admin.html: Portal tab logo field now has upload, "use account
  saved logo", remove, and preview, alongside the existing paste-a-URL
  input; upload/use-account-logo save immediately
- New public/logo-editor.js: opens a crop modal on file select for
  both account and portal logo uploads — auto-fit (contain, no
  cropping) by default, or a manual draggable/resizable square crop;
  re-encodes under 1.5MB client-side before upload