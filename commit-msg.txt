Add account profile fields+logo, topbar profile menu, click-to-open sidebar, and header styling fixes

- Account tab: business name, admin full name, digital address, country,
  business location fields with Save/Update button; new tenants columns
  (admin_full_name, digital_address, country, business_location) and
  GET/PATCH /api/dashboard/account-info

- Account/profile logo upload: multer (memory storage, 1.5MB limit,
  image/* only) encodes to a base64 data URL stored in new
  tenants.account_logo column; POST/DELETE /api/dashboard/logo

- Topbar profile button (desktop + new mobile-only bar): round icon
  (shows uploaded logo once set) opens a dropdown with "View profile"
  (business name, admin full name, email, country) and "Log out"

- Sidebar auto-hide redesign: replaces hover-expanding icon rail with a
  fixed hamburger-only 48px strip; click opens the full 220px sidebar,
  closes on mouse-leave or click-outside (touch/keyboard safe); pinned
  mode unchanged

- Header styling: .page-header (and print.html's .print-head) now bold,
  uppercase, letter-spaced across all shell pages; extended the same
  centered logo+title treatment to agent.html, which sits outside the
  admin shell and previously had no header treatment

New dependency: multer (^1.4.5-lts.1) - run npm install.
Schema changes are additive/idempotent - run npm run migrate.